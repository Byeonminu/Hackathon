<!DOCTYPE html>
<html>
<head>
    <title>포개다 스터디 페이지</title>
    <link rel="stylesheet" href="/stylesheets/style.css">

</head>

<body>
    <div class="auth" style="font-size:20px; color:white"> <%= user_nickname %> </div>
    <% let islogined; %>
        <% if(user_nickname !='' ){ %>
    <div class="auth"><a href="/auth/logout" class="lettering" style="font-size:20px">logout</a></div>
            <% } else{ %>
    
                <% } %>
                
    <h1 class="title" style="margin-top:0"><a href="/" class="lettering" style="font-size:40px">포스텍 개발자 다모임</a></h1>
    <h4 class="auth" style="margin-top:0; margin-bottom:0; padding-bottom:10px; font-size:20px;"><a href="/study/new" class="lettering">새 스터티 만들기</a></h4>
    
    <% for(var i=0; i < studylist.length; i++) { %>
    <% var user="/study/" +studylist[i].id; %>

        <br>
        <p>
            <li><a href= <%=user %>> <%= studylist[i].title %> </a>

                <%= " - " + studylist[i].createat.toLocaleString() %>
            </li>
        </p>
        <hr />

        <% } %>

</body>


</html>