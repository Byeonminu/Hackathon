<!DOCTYPE html>
<html>
<head>
    <title><%= writing.title %> 스터티 모집</title>
    <link rel="stylesheet" href="/stylesheets/style.css">

</head>

<body>
    <div class="auth" style="font-size:20px; color:white"> <%= user_nickname %> </div>
    <% let islogined; %>
        <% if(user_nickname !='' ){ %>
            <div class="auth"><a href="/auth/logout" class="lettering" style="font-size:20px">logout</a></div>
            <% } else{ %>
    
                <% } %>

    <h1 class="title" style="margin-top:0"><a href="/" class="lettering" style="font-size:40px">포스텍 개발자 다모임</a></h1>
    <h4 class="auth" style="margin-top:0; margin-bottom:0; padding-bottom:10px; font-size:20px;"><a href="/study/" class="lettering">스터디 모집</a></h4>


    <div class="signin_box">
        <fieldset class="sign">
            <h2 style="text-align: center"><%= writing.title %></h2>
            <div style="text-align: right">
                <%= "작성자 : " + writing.nickname %>
                <br/>
                <%= "작성 날짜 : " + writing.createat.toLocaleString() %>
            </div>
            <div style="text-align: center; margin-bottom: 30px;">
                <%= writing.description %>
            </div>
            
            <div style="text-align: right">
            <form action="/study/apply" method="post">
                <input type="hidden" name="study_id" value= "<%= study_id %>">
                    <input type="submit" value="지원하기">
            </form>
           
            </div>
            <div>
                <% for(var i=0; i < applicants_list.length; i++) { %>
                        <br>
                        <p>
                            <li> <%= (i+1) %>번 지원자 <%= ' 이메일: ' + applicants_list[i].applicant_email %> </a>
                
                                <div style="float:right; margin-right:10px"><%= applicants_list[i].applicant_nickname%>, <%= applicants_list[i].applyat.toLocaleString() %></div>
                            </li>
                        </p>
                        <hr />
                
                        <% } %>
            </div>
</div>
    


</body>


</html>