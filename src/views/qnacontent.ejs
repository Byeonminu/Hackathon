<!DOCTYPE html>
<html>
<head>
    <title><%= questions.title %> 질문</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="auth" style="font-size:20px; color:white"> <%= user_nickname %> </div>
    <% let islogined; %>
        <% if(user_nickname !='' ){ %>
            <div class="auth"><a href="/auth/logout" class="lettering" style="font-size:20px">logout</a></div>
            <% } else{ %>
    
                <% } %>


<h1 class="title" style="margin-top:0"><a href="/" class="lettering" style="font-size:40px">포스텍 개발자 다모임</a></h1>
<h4 class="auth" style="margin-top:0; margin-bottom:0; padding-bottom:10px; font-size:20px;"><a href="/qna" class="lettering">질문 게시판</a></h4>

<div class="signin_box">
    <fieldset class="sign">
        <h2 style="text-align: center"><%= questions.title %></h2>
        <div style="text-align: right">
            <%= "작성자 : " + questions.nickname %>
            <br/>
            <%= "작성 날짜 : " + questions.createat.toLocaleString() %>
        </div>
    
        <div class = "real_description" style="text-align: center; margin-bottom: 30px;">
            <%= questions.description %>
        </div>
        <br/>
        <form action="/qna/new-comment" method="post">
            Comment
                <input type="hidden" name="qna_id" value="<%= qna_id %>">
                <p><textarea id= "comment_box" class="desc" name="comment" placeholder="댓글 달기"> </textarea></p>
                <input type="submit" value="댓글 작성하기">
        </form>
    </fieldset>
    
    <div><% for(var i=0; i < comments.length; i++) { %>
    
            <br>
            <p>
                <li> <%= (i+1) %>번 댓글  &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <%= comments[i].description %>
    
                <div style="float:right; margin-right:10px"> <%= comments[i].nickname %> ,  <%=  comments[i].createat.toLocaleString() %> </div>
                </li>
            </p>
            <hr />
    
            <% } %>
    </div>
</div>
</body>


</html>